{
  "manifest_version": 3,
  "name": "Bookmark Batch Fixer",
  "version": "1.1",
  "description": "Scan, check, and batch fix bookmarks: delete dead links, upgrade HTTP to HTTPS",
  "permissions": ["bookmarks", "downloads", "storage", "unlimitedStorage"],
  "host_permissions": ["<all_urls>"],
  "action": {
    "default_popup": "popup.html"
  }
}
